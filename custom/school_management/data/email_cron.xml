<odoo>
    <record id="cron_attendance_absent_alert" model="ir.cron">
        <field name="name">Attendance Absent Alert</field>
        <field name="model_id" ref="model_attendance_management"/>
        <field name="state">code</field>
        <field name="code">model.send_absent_alerts_cron()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>
</odoo>
